# v0.5 Camera Semantic HIL Appendix

The generic sensor evidence layer now has a dedicated camera reference bridge.

```text
REAL
camera + edge compute + perception runtime

SIMULATED
plant + actuator / robot body
```

The camera bridge preserves model identity, frame references, inference confidence, and semantic state provenance. Low-confidence inferences become `not_observable` rather than guessed semantic facts.

This appendix does not record a real camera deployment result. The next evidence step is to run a deployment producer on an intended edge host and retain the camera frames, inference records, runtime evidence, and resulting HIL report.
