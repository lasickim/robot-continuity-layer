# RCL v0.5 Development Line

**Development version:** `0.5.0-dev` / package metadata `0.5.0.dev0`

RCL v0.5 begins from the frozen v0.4 checkpoint and shifts the primary engineering goal from capability-surface growth to progressively stronger continuity evidence.

## Boundary from v0.4

The v0.4 checkpoint remains historical and unchanged. Its `.rcl` package-format compatibility boundary remains the existing `0.2` five-payload archive format.

v0.5 does not reinterpret synthetic observations as physical evidence. Instead it uses an explicit validation ladder:

```text
Phase 1 — deterministic semantic simulation
        ↓
Phase 2 — hardware-in-the-loop
        ↓
Phase 3 — low-cost physical Robot A / Robot B
        ↓
Phase 4 — richer manipulation / human-interaction validation
```

## Phase 1 — complete reference harness

The first v0.5 reference experiment is documented in [`V0.5_SIMULATION_REFERENCE.md`](V0.5_SIMULATION_REFERENCE.md).

It tests one of RCL's central claims:

> Robot B may use a different HOW from Robot A while preserving the same declared WHY.

The reference fixture uses:

```text
Robot A
rearward attention + clearance classifier

Robot B
direct sitting-area clearance state
```

Both are evaluated against the same required Intent across repeated synthetic sessions. The experiment verifies that the target-selected capability path and target-native strategy agree with the target observations.

Robot B intentionally cannot reproduce Robot A's optional rearward-look expression. Functional Intent migration may therefore pass while expression migration reports `unsupported`.

## Phase 2 — HIL readiness and sensor bridge

The base HIL contract is documented in [`V0.5_HIL_REFERENCE.md`](V0.5_HIL_REFERENCE.md).

The first boundary is:

```text
REAL
edge compute / controller execution host

SIMULATED
plant
```

The HIL harness reuses the Phase-1 semantic experiment but adds an explicit runtime attestation and evidence-grade gate. A CI run or ordinary unclassified host cannot claim HIL merely because semantic assertions pass.

The next Phase-2 layer is documented in [`V0.5_DISTANCE_SENSOR_HIL.md`](V0.5_DISTANCE_SENSOR_HIL.md):

```text
REAL
edge compute
+ distance sensor

SIMULATED
plant
+ actuator / robot body
```

Here, target-side Intent observations are derived from measured `distance_mm` values rather than a prewritten target observation fixture. The sensor driver stays outside RCL core and writes the portable `distance-sensor-reading-set` boundary.

The runtime attestation remains intentionally marked `self_declared`; it is not cryptographic device identity or physical safety certification.

## Evidence labels

For v0.5 development, evidence provenance must remain explicit:

```text
SIMULATION
→ deterministic or simulator-produced evidence

HIL
→ real compute/sensor/controller components with simulated or partial plant

PHYSICAL
→ measurements produced by actual robot hardware in a declared experiment context
```

Synthetic evidence must never be relabeled as physical evidence merely because it passes the same semantic evaluator.

## Current next step

Execute the distance-sensor HIL bridge on an intended edge computer with an actual range sensor and retain:

```text
raw sensor log
+ distance-sensor-reading-set
+ runtime/evidence refs
+ distance-sensor-hil-report
```

Only after genuine deployment measurements exist should the project record the first real `HIL` evidence artifact.

After that, move the next boundary into hardware:

```text
real edge compute
+ real sensor
+ real controller or actuator interface
+ simulated/partial plant
```

A complete physical Robot A / Robot B remains Phase 3 rather than a prerequisite for continuing v0.5 development.
